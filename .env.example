# Environment variable examples (DO NOT commit secrets)
# Add values in Vercel Dashboard -> Project -> Environment Variables

# NEXT_PUBLIC_API_BASE  -> public frontend can read this (prefix NEXT_PUBLIC_)
# NEXT_PUBLIC_API_BASE=https://api.example.com

# SECRET_API_KEY  -> server-only secret for Next.js server-side code
# SECRET_API_KEY=
