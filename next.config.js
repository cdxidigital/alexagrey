// Next.js configuration - tweak as needed
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any environment-based rewrites/proxies here in the future
}

module.exports = nextConfig
